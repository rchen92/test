groupadd -g 1001 oinstall
useradd -u 501 -g oinstall  oracle

groupadd -g 1004 oinstall
groupadd -g 1002 dba
groupadd -g 1003 oper
useradd -u 501 -g oinstall  oracle
usermod –a –G oinstall sge


# 设置密码
passwd oracle


setenforce 0

修改/etc/selinux/config
selinux=disabled

systemctl stop firewalld
systemctl disable firewalld.service


mkdir -p /app/oracle
chown -R oracle:oinstall /app/
chmod -R 775 /app


安装依赖包
gcc.x86_64
libaio-devel.x86_64
elfutils-libelf-devel.x86_64
glibc-devel.x86_64
gcc-c++.x86_64
libstdc++-devel.x86_64



环境变量设置
export ORACLE_BASE=/app/oracle
export ORACLE_HOME=/app/oracle/product/11.2.0/client_1
export NLS_DATE_FORMAT="yyyy-mm-dd hh24:mi:ss"
#export NLS_LANG="SIMPLIFIED CHINESE_CHINA.ZHS16GBK"
export LD_LIBRARY_PATH=$ORACLE_HOME/lib
# instantClient安装模式,oracle home下没有bin目录，path可以不用添加
export PATH=$ORACLE_HOME/bin:$PATH 


3.1	更新client 安装包
在root用户下
xhost +


在oracle用户下
解压database安装包,到oracle用户 home目录
unzip p13390677_112040_Linux-x86-64_4of7.zip -d /home/oracle

设置DISPLAY环境变量
export DISPLAY=180.3.10.103:0.0  #180.3.10.103 为图形界面接收端IP

进入database安装包目录,执行./runInstaller










